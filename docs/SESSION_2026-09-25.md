# Сессия 2026-09-23 … 2026-09-25: итоги

Отчёт для продолжения работы в новой сессии. Сначала итог, потом детали, в конце — что делать дальше.

## Итог в пяти строках

1. **Исправление крена переделано и встроено в пайплайн** (`src/fix_pipeline.py`, `roll_adaptive`). Крен правится только там, где измерен дефект DJI, и теперь закрывает и медленное покачивание 1–4 Гц, которое старый фикс оставлял. На примере Pro подтверждено глазом («прям КРУТО — сильно лучше») и рендером.
2. **Остальные 16 клипов по крену не трогаются вовсе** (коррекция 0.000°): правило «не трогать там, где баг не найден».
3. **Собрана воспроизводимая база проблемных мест**: 17 клипов, 166 кусков, 626 событий, с индексом для разметки глазом. Её цель — тестовый набор для любых будущих правок.
4. **Проверено рендером Gyroflow**: из стадий старого пайплайна полезны только крен из картинки и удаление спайков. Винер, события и поправка на экспозицию нейтральны или вредят, больше всего на хороших клипах.
5. **Код не закоммичен.** Всё новое лежит в рабочей копии (список ниже).

## 1. Что изменилось в коде проекта

| файл | что |
|---|---|
| `src/fix_pipeline.py` | новая `roll_adaptive()` (+ `_band_gain`, `_running_rms`) заменяет прежнюю стадию крена (выше 4 Гц с весом R0=40); в отчёт добавлены `roll.defect_active_frac`, `roll.defect_touched_frac`; шапка модуля переписана |
| `src/main.py` | описание стадии крена |
| `README.md`, `README.ru.md` | описание крена и оговорка «ниже 1 Гц / 1–4 Гц» |
| `examples/o4pro_13-17s/*`, `examples/o4lite_53-58s/*` | пересобраны новым пайплайном: `_telemetry_fixed.mp4`, `_00_control.mp4`, отчёт, 4 PNG; README примеров обновлены |

Тесты: `cd tests && python -m pytest -q` → 25 passed, 1 skipped.

### Как устроен новый крен (`roll_adaptive`)

- **Выше 4 Гц** крен по картинке заменяет телеметрию, вес 1/(1+(скорость/120)²).
- **1–4 Гц** — только на спокойных кадрах: вес 1/(1+(скорость/15)²), и только если в пределах 1 с нет кадра быстрее 30 °/с. Полосовой фильтр растаскивает коррекцию на ~0.5 с, а картинка перед манёвром уже смазана и врёт.
- **Ниже 1 Гц** — телеметрия как есть.
- **Кадры быстрее 60 °/с** (±0.1 с) не используются.
- **Качество картинки**: две её оценки крена (kabsch и curl) должны сходиться выше 4 Гц (< 0.03°), и точек должно быть ≥ 300.
- **Коэффициент картинки** меряется в полосе 2–4 Гц.
- **Детектор дефекта**. Для каждой спокойной секунды считается робастная (MAD) ошибка крена «телеметрия − картинка» выше 4 Гц. Секунда правится, если медиана 7 ближайших спокойных секунд в пределах 20 с попадает в линейный переход 0.018–0.025°; если таких секунд меньше 5, секунда не правится. У `pro` значения 0.026–0.041°, у остальных 16 клипов 0.0065–0.012°.
- Отказались от требования «баг должен быть и в самой секунде»: у `pro` это отнимало коррекцию везде, где камера движется, а хорошие клипы и без него остаются по нулям.

### Числа (пример Pro, рендер Gyroflow, px у края 4K‑кадра)

| | покачивание 1–4 Гц (спокойно, 1–3 с) | дрожание 4–25 Гц | покадрово, весь кусок |
|---|---|---|---|
| оригинал | 3.65 | 1.85 | 2.32 |
| старый фикс | 3.32 | 0.78 | 1.09 |
| RA без защит (его вы видели и одобрили) | 1.82 | 0.55 | 0.82 |
| **новый пайплайн** | **2.33** | **0.56** | **0.87** |
| шум самого замера | ~0.6 | ~0.37 | ~0.47 |

По телеметрии (спокойные кадры): дрожание крена 0.1105 → 0.0132 °/кадр при пороге картинки 0.0074. Разница с одобренным вариантом в покачивании — цена защиты «спокойная окрестность»: в начале примера быстрый наклон, и секунда после него правится меньше. Именно эта защита убрала вред у манёвров на полном клипе.

Полный `pro` (92 с), покачивание внутри каждой спокойной секунды (метрика без протечки): сумма −11%, лучше 16 секунд из 29. Сильнее всего в эпизодах дрожания: 15 с было 4.7 px, стало 1.1. Файл для проверки глазом, собранный финальным пайплайном: `F:\36\RA_test\DJI_20260905181949_0005_D_telemetry_fixed.mp4` (+ `_00_control.mp4`, отчёт, overview). Крен: дефект на 100% клипа, коррекция 0.035° rms, пик 0.49°. Прежние стадии работают как раньше: 23 подтверждённых события, Винер.

## 2. Инструменты исследования (новое, `research/`)

| файл | назначение |
|---|---|
| `research/SEGMENTS.md` | протокол исследования: этапы, детекторы, пороги, правила |
| `research/clips.json` | реестр 17 клипов: путь, камера, дрон, линза и её профиль, прошивка, серийник, оценка на глаз |
| `research/diagnose.py` | этап 1: детекторы (spike / tel_jitter / render_jerk / image_bad), нарезка ±10 с без перекодирования с телеметрией, превью H.264, графики, индексы HTML/CSV, кэш, устойчивые id событий, сверка заголовка с реестром |
| `research/check_segments.py` | проверка базы: окна, кадры, кодеки, id, края 5 с |
| `research/summarize_base.py` | `SUMMARY.md` по базе: по клипам и группам (блок, поколение, глаз, прошивка) |
| `research/ab_bench/` | стенд A/B через рендер: `run_ab*.py`, `residual.py` (замер остатка в рендере), `fusion.py`, `spikefix.py`, `rolladapt.py` (прототип RA), `analyze_full.py`, `episodes.py`, `multigap.py`, `closure.py`, `teljitter.py` |

### Данные на диске (всё в `artifacts/`, в git не попадает)

- `artifacts/segments/` (68 ГБ): база, `index.html`, **`index_by_severity.html`** (для разметки), `index.csv`, `meta.json`, `SUMMARY.md`, `logs/`.
- `artifacts/cache/<клип>/` (34 ГБ): `image.npz`, `orig_render.mp4` (1080p, Gyroflow по умолчанию), `orig_resid.npz`. Пересборка базы из кэша занимает минуты.
- `artifacts/roll_test/`: все рендеры и замеры по крену, `compare_orig_fixed_RA.mp4` (бок о бок).

Команды:
```
python research/diagnose.py --out artifacts/segments --cache artifacts/cache --gyroflow F:\Gyroflow\Gyroflow.exe
python research/check_segments.py artifacts/segments
python research/summarize_base.py artifacts/segments
python research/diagnose.py --out artifacts/segments --reindex
```

## 3. Установленные факты

- **Телеметрия DJI в целом точная.** Выше 4 Гц она расходится с картинкой на 0.004–0.009° на 16 из 17 клипов.
- **Два разных дефекта.** (а) Дрожание и покачивание крена — только `pro` (0005, по словам пользователя новая камера на старом блоке, прошивка 01.00.06). У блока 2026 на той же прошивке его нет. (б) Одиночные спайки — у многих O4, в разной степени.
- **Картинка точная, но ненадёжная.** Шум одной пары кадров ≈ 0.003°, в 30–50 раз меньше ошибки DJI; базы 2–8 кадров ничего не добавляют (расхождение растёт линейно, это систематика). Но ниже 2 Гц мешает параллакс (рыск занижен до 0.55, утечка рыска в крен 0.25–0.3), а ветки, темнота и смаз дают ошибки. Проверка качества выше 4 Гц не видит ошибок картинки на 1–4 Гц.
- **Стадии старого пайплайна (A/B рендером на 4 клипах, из них 2 хороших).** Помогают крен и спайки. Винер портит тангаж (Pro 1.14, Lite 1.08–1.22). События портят тангаж на Lite и вредят хорошему клипу. Прототип «картинка на всех трёх осях» не лучше. Правило «не навреди» проходит только ядро «крен + спайки».
- **Окно на 5 кадров (цепочка LK)** заметно шумнее суммы пяти парных оценок, а на нём построена стадия событий.
- **Спайки в минуту** (до разметки): блок Lite 6.2 (на двух рамах), блок 2026 (P1R) 4.4, блок 2025 (N58) 2.0 (0.9–3.7), дом 0.4, O3 0.2. Гипотеза «спайки = новый гироскоп» наводящая, но не доказана: рамы в парах разные, у N58 18.07 было 3.7.
- **O3 (FC8383, `dvtm_wm169`)**: наш разбор совпадает с Gyroflow бит в бит; спайков почти нет, дрожания крена нет.
- **Серийник относится к блоку передачи, не к камере** (поправка пользователя).

## 4. Отвергнуто

- Ошибка крена ниже 4 Гц — по всем клипам: там картинка сама ненадёжна (параллакс). Правится только на спокойных кадрах у клипа с дефектом.
- Перекос осей телеметрии относительно камеры: перекрёстные члены есть только ниже 2 Гц, это параллакс.
- Шум внутри кадра и ступеньки на стыках блоков сэмплов: максимум 0.02° (≈0.5 px); «ступенька на стыке» оказалась мусорным блоком в конце файла.
- «Больше коррекции — лучше»: прототип fusion (все оси выше 2/4 Гц) не лучше и вредит хорошим клипам.
- Длинные базы (2–8 кадров) для точности: по шуму ничего не дают.

## 5. Найденные ошибки (в коде, кроме уже исправленного крена, **не исправлены**)

- `fit_roll_axis`: при корреляции < 0.9 подставляется `rollfix.U`, но возвращается фактическая корреляция; `build()` проверяет её по порогу ≥ 0.85 и на клипе 0008 использует константу как измеренную модель (u_y −0.28 при реальном ≈0). После замены стадии крена эта модель уже ни на что не влияет, кроме поля `disc` в отчёте и диагностике.
- Коэффициенты изображения считаются по всем частотам на быстрых кадрах, где их определяет параллакс (0.55–0.7), а применяются там, где коэффициент ≈1: `fine` для событий (перелёт в 1.5–1.8 раза), калибровка Винера.
- Если калибровка Винера не прошла, он откатывается на кривую `KEEP_PANTILT`, снятую на одном клипе.
- `build(events=False)` выключает заодно и спайки.
- `trim_video.py` не режет O3: переменный шаг времени у `djmd`, дорожка `nail`. В базе O3 есть только превью и графики.

## 6. Ловушки методики (не наступать повторно)

- **Рендер‑метрика** надёжна только на спокойных кадрах (< 45 °/с). На быстрых один и тот же рендер при разных настройках трекера даёт числа, различающиеся в 2 раза.
- **Полосовой фильтр по положению за весь клип протекает**: огромное движение манёвра попадает в соседние секунды. Посекундно считать локально: положение внутри секунды минус квадратичный тренд.
- **Ниже 4 Гц рендер‑метрика и коррекция используют один и тот же метод** (поворот по картинке) — это круговая проверка. Выше 4 Гц такой проблемы нет.
- «Худшие секунды» хорошего клипа — это сцена (ветки, темнота, посадка), а не телеметрия.
- **Взлёт и посадка** (первые и последние 5 с) исключаются из анализа (правило пользователя).
- **CLI `-g` в Gyroflow** обнуляет время считывания, и коррекция rolling shutter выключается. Для рендера патчить копию видео (`dji_o4.py patch`).
- Превью через OpenCV `mp4v` в браузере не играют; писать H.264 через PyAV (`H264Writer`).
- Windows не различает регистр имён: `x.mp4` и `x.MP4` — один файл.
- Шаг цепочки в фоне нельзя ждать по появлению перенаправленного файла вывода: файл появляется в момент запуска.
- Gyroflow пишет `.gyroflow` рядом с видео при экспорте, их нужно удалять.

## 7. Открыто и что дальше

1. **Разметка глазом** в `artifacts/segments/index_by_severity.html`: хотя бы спайки и верх списка, затем «Export CSV». Это закроет гипотезу про спайки и даст тестовый набор.
2. **Какая камера с новым гироскопом**: `F:\34\20` (блок 9F2KP1R) или `F:\34\20pro` (блок 9F2KN58, тот же, что crux35)? И оценка на глаз для клипов с `unknown` в реестре.
3. **Проверить глазами полный `pro`**: `F:\36\RA_test\DJI_20260905181949_0005_D_telemetry_fixed.mp4`, особенно 24 и 56 с.
4. **Остальные стадии** (Винер, события, покадровая поправка на экспозицию) по данным не помогают и нарушают правило «не трогать без найденного бага». Пользователь просил минимум изменений, поэтому они оставлены как есть; решить, убирать ли их.
5. **Спайки**: правило с проверкой качества картинки (`research/ab_bench/spikefix.py`) на Lite совпадает с текущим. Встроить проверку качества в `detect_spikes` — по желанию.
6. **Закоммитить**: `src/fix_pipeline.py`, `src/main.py`, README, `examples/*` (PNG, JSON, README), `research/*`, `docs/SESSION_2026-09-25.md`.

Память ассистента по проекту: `ab-render-results`, `segments-base`, `roll-adaptive`, `ideas-over-edits`, `gyrogate-context` (исправлена поправка про серийник).
